---
interface Props {
  class: string
}

const { class: classStyle }: Props = Astro.props
---

<div class={`${classStyle} w-72 h-72 opacity-20 bg-gradient-to-br from-accent to-primary rounded-full blur-3xl`} />
